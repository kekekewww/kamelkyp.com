# Task 1 report — cloud foundation

Status: complete.

## Scope delivered

- Pinned Node to 24 (`.node-version`) and constrained the package engine to `>=24 <25`.
- Added the specified npm package manifest, Biome configuration, TypeScript configuration, React Router and Vite configuration, and ignore rules.
- Added the stable `SERVICE_IDS`, `ServiceId`, and `isServiceId` contract.
- Added the prescribed ServiceId unit test before its implementation.
- Added `vitest.config.ts` as the minimal bootstrap required for the prescribed `test:unit` script to execute.
- Added `.github/workflows/lockfile.yml` with `workflow_dispatch` and a push trigger limited to `codex/01-cloud-foundation`. Both jobs are guarded by `if: github.actor != 'github-actions[bot]'`, preventing a bot-created lockfile commit from recursively running the workflow.
- Generated and committed `package-lock.json` in GitHub Actions, not on a local machine.

## TDD evidence

### RED

[Refresh lockfile run 2](https://github.com/kekekewww/kamelkyp.com/actions/runs/31397463826) completed with the `lockfile` job passing and the `unit` job failing as expected.

The unit step ran `npm run test:unit -- tests/unit/service-id.test.ts` and failed with `ERR_MODULE_NOT_FOUND`: `Cannot find module '../../app/lib/services/service-id'`. This was the expected pre-implementation failure. The same run generated the initial `package-lock.json`; commit [`22b1ad2`](https://github.com/kekekewww/kamelkyp.com/commit/22b1ad2cb5f8cccea5c49aab757db77fceb25728) was authored by `github-actions[bot]`.

### GREEN

[Refresh lockfile run 5](https://github.com/kekekewww/kamelkyp.com/actions/runs/31398002524) completed successfully. Both jobs passed:

- `lockfile`: `npm install --package-lock-only --ignore-scripts` and the commit check.
- `unit`: `npm ci`, `npm run test:unit -- tests/unit/service-id.test.ts`, and `npm run format:check`.

## Commits

Implementation branch `codex/01-cloud-foundation`:

- [`43df224`](https://github.com/kekekewww/kamelkyp.com/commit/43df22490edcaeafa0916397258f0fc86098d0c4) — `chore: bootstrap pinned toolchain`
- [`8be892b`](https://github.com/kekekewww/kamelkyp.com/commit/8be892b34ef6ee264b8e7f2a516a2bc3863a075d) — `chore: add cloud unit-test bootstrap`
- [`6a453f8`](https://github.com/kekekewww/kamelkyp.com/commit/6a453f82b1c46cfe87912990f48cff9f2110583c) — `chore: add cloud lockfile workflow`
- [`ae5a33c`](https://github.com/kekekewww/kamelkyp.com/commit/ae5a33c16ba080b9c595dd51869ef82a9773a620) — `test: define stable service identifiers` (RED commit)
- [`22b1ad2`](https://github.com/kekekewww/kamelkyp.com/commit/22b1ad2cb5f8cccea5c49aab757db77fceb25728) — `chore: refresh npm lockfile` (GitHub Actions bot)
- [`7dde7f8`](https://github.com/kekekewww/kamelkyp.com/commit/7dde7f8832399bd5e1339a0d3c91eda3a2a391e6) — `feat: scaffold React Router 8 Cloudflare project`
- [`34f1e17`](https://github.com/kekekewww/kamelkyp.com/commit/34f1e179f492d8bf5bf2a559873bf98ddb54680a) — `test: verify scaffold checks in GitHub Actions`
- [`d11cc15`](https://github.com/kekekewww/kamelkyp.com/commit/d11cc154c45389c03568d8ba0b42bc1ef6ba852b) — `style: organize service identifier imports`

## Changed files

- `.node-version`
- `.gitignore`
- `.github/workflows/lockfile.yml`
- `app/lib/services/service-id.ts`
- `biome.json`
- `package.json`
- `package-lock.json` (cloud-generated by GitHub Actions)
- `react-router.config.ts`
- `tests/unit/service-id.test.ts`
- `tsconfig.json`
- `vite.config.ts`
- `vitest.config.ts` (required bootstrap for the declared unit-test script)

## Self-review

- Confirmed the ServiceId array order exactly matches the four approved identifiers and that `isServiceId` narrows only those identifiers.
- Confirmed RED occurred before `service-id.ts` existed and the final GREEN run passed the focused unit test and formatting check using `npm ci`.
- Confirmed the first lockfile was generated remotely and committed by `github-actions[bot]`.
- Confirmed the push trigger is restricted to the implementation branch and bot pushes are guarded, so the lockfile commit does not recursively execute jobs.

## Concerns

- The task brief’s declared `test:unit` script references `vitest.config.ts` but does not list that file; the minimal config was added to make the required RED/GREEN command executable.
- This task validates the requested focused unit test and format check only. `npm run check` is not yet expected to pass because later foundation tasks have not added the Worker test config, Wrangler base configuration, application routes, and build inputs. The initial install output also reported six dependency-audit findings; no automatic upgrade was applied because versions are explicitly pinned by the brief.
